import 'package:flutter/material.dart';

class MyShuttlesScreen extends StatelessWidget {
  const MyShuttlesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("My Shuttles screen here"),
      ),
    );
  }
}
